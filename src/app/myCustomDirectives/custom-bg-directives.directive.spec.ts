import { CustomBgDirectivesDirective } from './custom-bg-directives.directive';

describe('CustomBgDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomBgDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
